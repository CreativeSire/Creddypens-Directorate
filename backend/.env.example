# Postgres
# Example:
# If using the repo's `docker-compose.yml`, Postgres is exposed on localhost:5433
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5433/creddypens

# CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# LLM mock mode (0/1). Defaults to 0 when unset.
LLM_MOCK=0

# Supabase (optional; for later auth/storage and PostgREST access)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Provider API keys (used by LiteLLM when you enable chat for live agents)
ANTHROPIC_API_KEY=
# Google Gemini is commonly GOOGLE_API_KEY or GEMINI_API_KEY depending on your setup.
GOOGLE_API_KEY=
GEMINI_API_KEY=
XAI_API_KEY=

# Optional: model identifiers shown on dossiers (routing still controlled server-side)
ANTHROPIC_SONNET_MODEL=
ANTHROPIC_OPUS_MODEL=
GEMINI_PRO_MODEL=
GROK_FAST_MODEL=
GROK_REASONING_MODEL=

# Monitoring (optional)
SENTRY_DSN=
